name: Run Ansible Playbooks

on: push


jobs:
  run-playbooks:
    runs-on: ubuntu-20.04
    defaults:
      run:
        working-directory: ansibl
    steps:
    - uses: actions/checkout@v2
    - name: Run Ansible Playbook to deploy my blog
      run: ansible-playbook --verbose ansible/deploy.yaml

          